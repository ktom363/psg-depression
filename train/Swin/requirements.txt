# Python 3.11 recommended. These are dependencies, not a frozen tested lockfile.
# Install a torch build matching the local CUDA version; see pytorch.org.
torch>=2.1,<3
torchvision>=0.16,<1
timm>=0.9,<2
scikit-learn>=1.3,<2
tqdm>=4.65,<5
